export const dynamic = "force-dynamic";
import Home from "./components/Home/Home";

const Page = async () => {
  return (
    <main className=" ">
      <Home />
    </main>
  );
};

export default Page;
